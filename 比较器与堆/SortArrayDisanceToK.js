  
// 没有排好序啊 

const MinHeap = require('../heap/MinHeap');
const SortArrayDistanceToK = (originalArray, k) => {
  // 1.生成一个小根堆
  // const minHeap = new MinHeap();
  // let index = 0;

  // for(; index <= Math.min(originalArray.length - 1, k); index++) {
  //   minHeap.add(originalArray[index])
  // }

  // let i = 0;
  // for(; index< originalArray.length; i++, index++) {
  //   minHeap.add(originalArray[index]);
  //   originalArray[i] = minHeap.poll();
  // }

  // // 最后将小根堆中的数弹出就行了  
  // while(!minHeap.isEmpty()){
  //   originalArray[i++] = minHeap.poll();
  // }
}

